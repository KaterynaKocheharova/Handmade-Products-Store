import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    customColors: {
      mainBgColor: '#f0f0f0',
      secondaryBgColor: '#e1e2e6',
      mainFontColor: '#4a4a4a',
      secondaryFontColor: '#787775',
      accentColor: '#c4b3a9',
      highlightColor: '#b0a296',
    }
  }
});
