// if need to adjust styles for swipers in other component, the classes should be base + add extra class
// write a function that will take a swiper type as a prop and return different extra classes. In main component, 
// us clsx